# NextDay Biz Funding — frontend env
# Copy this file to `.env.local` for local dev, or set these in Vercel
# Project Settings → Environment Variables for production.

# Base URL of the backend API (the GCP VM running Caddy + Node).
# Defaults to the production URL if unset.
#
# Pre-DNS (current):
VITE_API_URL=https://136-119-104-124.nip.io

# Post-DNS (when api.nextdaybizfunding.com resolves to the VM):
# VITE_API_URL=https://api.nextdaybizfunding.com
